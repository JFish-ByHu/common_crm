# Common CRM 开发约定

## 命令执行

- Windows shell 命令统一使用 PowerShell 7（`pwsh.exe`）执行，不使用 `cmd.exe` 或 Windows PowerShell 5.1。
- 工具调用时显式指定 PowerShell 7 shell；若当前 PowerShell 7 安装路径包含版本号，以本机实际安装路径为准。

## 前端目录与组件化

- 新业务页面遵循 `src/views/<业务名>/index.vue`、`components/`、`hooks/`、`types.ts` 的结构，参考 `src/views/login`。
- 应用级布局遵循 `src/layout/index.vue` 作为编排入口，具体业务区域放在 `src/layout/components/`。
- 入口文件负责路由、状态和数据流编排；可复用或职责单一的视图区域拆为组件，通过 props/emits 传递数据和事件。
- 同一作用类型下的多个模块（例如 stores、services、hooks、utils）必须提供目录级统一入口 `index.ts`；业务代码优先从统一入口导入，避免散落引用具体实现文件。
- 优先使用 Element Plus 组件，只有被两个或以上页面或应用复用时才提升到 `packages`。
- 详情展示默认优先使用 Element Plus 抽屉组件 `el-drawer`；只有用户明确要求时才使用弹窗或其他展示方式。
- 详情字段优先使用无边框的 `el-descriptions` / `el-descriptions-item` 基础样式，不使用带网格边框的表格样式，除非用户明确要求。

## 前端反馈与提示

- 统一从 `@common-crm/utils` 导入封装的 `Message`、`Notification`；业务代码不直接调用 `ElMessage`、`ElNotification`，不自行创建另一套提示封装。
- 按场景选择组件，不按文案长度或开发者偏好混用。`Message` 用于当前操作的简短、轻量反馈；`Notification` 用于操作受阻、请求失败或需要用户进一步处理的问题。

| 场景                             | 使用方式                                     | 示例                                                                       |
| -------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------- |
| 用户主动操作成功                 | `Message.success`                            | 保存成功、删除成功、复制成功、登录成功、已退出登录                         |
| 无需请求的轻量操作提醒           | `Message.info` / `Message.warning`           | 请先选择记录、没有需要保存的更改                                           |
| 接口操作或数据加载失败           | `Notification.error`                         | 保存失败、列表加载失败、网络异常、超时、服务不可用；包括登录及退出登录失败 |
| 权限不足或会话失效               | `Notification.warning`                       | 没有该操作的权限、登录已过期；按接口错误码或 HTTP 状态判断，不匹配错误文案 |
| 配置缺失导致功能暂不可用         | `Notification.info` / `Notification.warning` | 该菜单暂未配置页面、请联系管理员完善配置                                   |
| 可定位到字段的输入校验           | `el-form` / `el-form-item` 校验              | 必填、邮箱格式、密码长度；不额外弹出 Message 或 Notification               |
| 删除、停用、强制登出等操作前确认 | `ElMessageBox.confirm`                       | 用户确认后才发请求；确认弹窗不替代操作结果提示                             |
| 需要持续展示的静态说明           | 普通文本或 `el-alert`                        | 平台管理员权限范围说明；不得用来展示接口失败或权限不足警告                 |

- `Notification` 默认使用简短标题和具体原因，例如 `{ title: '保存失败', message: '...' }`；使用可公开展示的后端业务提示或前端兜底文案，不展示堆栈、SQL、凭据等内部信息。普通接口错误不使用 `Message.error`。
- 同一失败只提示一次：请求层、会话处理层与页面明确提示归属，不重复弹窗；已经统一处理的会话失效，页面不再次提示。心跳、轮询等后台请求不得逐次弹出通知，同一持续故障应合并或限频。
- 用户取消确认框、主动中止请求、切换页面导致请求取消时不提示失败；组件卸载后不再弹出该组件请求的结果通知。
- 成功提示必须在操作请求成功后出现；需要刷新列表、关闭弹窗或跳转时，等待相关界面更新完成再提示。普通查询、重置、自动刷新及心跳成功不弹成功提示。
- 操作失败保留用户已输入的表单内容，不关闭编辑弹窗；请求错误不额外渲染表单顶部 `el-alert`。字段校验、静态说明与请求错误提示各自承担独立职责。
- 本规范作为后续新增及修改代码的基准；涉及历史不一致写法时按本规范同步调整，不为提示风格单独扩大无关业务修改范围。

## 函数与方法命名

- 前端与共享包中的普通函数统一使用箭头函数；NestJS Controller、Service 等依赖类方法、装饰器或框架生命周期的场景保留标准类方法写法。
- 所有函数、方法及承载函数的变量或属性均禁止以 `handle` 开头，适用于前端、后端和共享包，事件回调也遵循此规则。
- 名称必须表达具体业务语义，优先使用“动作 + 对象”的小驼峰形式，例如删除用户 `deleteUser`、选择用户 `selectUser`、查询用户列表 `queryUserList`、切换账号状态 `updateAccountStatus`。
- 禁止使用 `handleClick`、`handleSubmit`、`handleChange`、`handleDeleteUser` 等名称；应根据实际职责命名为 `selectUser`、`createUser`、`updateAccountStatus`、`deleteUser` 等，不能只替换前缀而保留模糊语义。
- 重命名时同步更新调用方、模板事件绑定及相关导出，保持名称与实际行为一致。

## 后端接口命名

- 新增和调整业务接口时采用 `/资源/动作` 路径，明确表达操作语义，不仅依靠 HTTP 方法区分同一路径的增删改查。
- 统一使用 `list` 查询列表、`selectList` 查询下拉选项、`create` 创建、`update` 编辑、`delete` 单删、`batchDelete` 批删；创建统一使用 `create`，不混用 `add`。
- 其他动作使用语义明确的小驼峰名称，例如 `updateAccountStatus`。查询使用 GET 和 query 参数，创建使用 POST，编辑使用 PATCH，删除使用 DELETE；写操作所需的 ID 与其他参数一并放入 JSON 请求体。
- 接口调整时同步 Controller 注释、DTO、调用方和接口文档；已有接口未在本次修改范围内时不批量改名。

## 业务错误码

- 错误目录唯一来源为 `packages/errors/src/`，按 `common`、`auth`、`users`、`roles`、`menus`、`files` 拆分，通过 `@common-crm/errors` 引用；业务代码不得硬编码公开错误文案或直接比较六位数字。
- 错误定义包含稳定的六位 `code`、独立的 `httpStatus` 和 `msg`。前两位为模块号（10 通用、11 认证、12 用户、13 角色、14 菜单授权、15 文件），后四位递增；编号不复用、不随文案变化而修改，新模块先分配新号段。
- 后端通过 `BusinessError` 或继承它的模块异常抛出错误，由 `HttpExceptionFilter` 统一返回 `{ code, data: null, msg }` 和对应 HTTP 4xx/5xx；禁止用 HTTP 200 承载新业务失败，也禁止把业务 code 当作 HTTP 状态。
- 动态文案使用错误目录中带类型的参数函数，参数只传公开业务信息；未知异常和底层故障统一兜底，不把 SQL、路径、堆栈等内部信息返回客户端。字段格式校验复用通用校验错误码。
- 前端依据共享错误常量或 HTTP 状态处理逻辑，默认展示后端 `msg`，不根据中文文案分支，也不将所有 409 都解释为同一种冲突。请求错误统一通过 `notifyRequestError`，会话失效由请求层配合 `notifySessionExpired` 处理，避免重复提示。
- 错误包运行时代码使用带 JSDoc 类型约束的原生 ESM，兼容前端与 Node.js 22.12+ 的 CommonJS 加载；`types/` 由 `pnpm --filter @common-crm/errors typecheck` 生成并纳入版本管理，不手改声明。该命令同步检查编号唯一性、模块号段和 HTTP 状态；根目录 typecheck 已包含此检查。

## 后端模块组织

- 业务模块在 `crm_serve/src/modules/` 下平级组织，例如 `auth`、`presence`、`roles`、`users`。
- 模块根目录保留 `*.module.ts`、`*.controller.ts`、`*.error.ts`、`types.ts`、`index.ts` 和说明文档；单个响应转换文件 `*-result.presenter.ts` 也放在根目录，出现多个相关实现后再按需建立 `presenters/`。
- 常规业务模块保留 `services/`、`repositories/`、`dto/` 等核心分类；像 `presence` 这样只有一个服务的小模块，服务可直接放在根目录，规模增长后再拆分。
- 已有或用户明确要求新增的测试放在 `tests/`；`guards/`、`decorators/` 等目录按需创建，不为单个辅助文件机械建目录，不创建空目录。
- 同类实现目录提供 `index.ts`。跨目录优先从统一入口导入，同目录内部依赖直接引用实现，避免通过自身入口产生循环依赖；测试可直接引用和 mock 被测实现。
- 模块根入口只导出跨模块使用的能力，不批量导出内部 Controller、DTO、错误或仓储；Nest Module 的 `exports` 同步维护运行时可注入能力。
- 保持 Controller → Service → Repository 的调用关系；数据库连接等基础设施留在 `src/database/`，业务仓储留在所属业务模块内。
- 前后端公开 API 请求和响应类型统一维护在 `packages/types/src/api/`，通过 `@common-crm/types/api` 进行纯类型导入；后端 DTO 保留运行时校验，公开返回值显式对齐共享类型。数据库内部记录、密码摘要、JWT 载荷不放入公共契约。
- 后端跨模块的通用参数转换和分页逻辑优先复用 `src/common`；不让后端依赖包含 Vue、Element Plus 或浏览器请求逻辑的共享包。

## 菜单与角色权限

- 权限标识由菜单管理手工配置，角色关联菜单与按钮，用户通过角色获得权限；禁止在业务 Controller 中写死权限标识。
- 业务接口默认受全局权限守卫保护，按实际 HTTP 方法和路由模板匹配数据库映射；新增接口同步配置权限规则。
- 前端按钮直接绑定手工配置的 `permissionCode`，通过 `hasPermission` 判断权限；表格操作使用 `permission` 配置，不维护页面操作键清单。权限标识改名时同步修改前端绑定，角色关联仍按按钮 ID 保留。
- `componentKey` 仅用于动态路由加载子应用预注册组件，不参与按钮权限映射。
- 子应用路由页面在 `src/views/**/*.vue` 的 `<script setup>` 顶层使用一次 `definePage({ key, title, defaultPath, visible? })`，仅允许静态字符串/布尔字面量；`index.vue`、`detail.vue`、`edit.vue` 均可声明。抽屉、表单等内部组件不声明，`components/`、`hooks/`、`tests/` 不扫描。
- 页面清单及懒加载映射由 `@common-crm/dev-tools` 的 `crmPages()` 自动生成，禁止再维护手写公共页面清单或重复组件映射。组件 key 使用应用名前缀且保持稳定，文件移动不更换已绑定的 key。
- 宏只提供页面能力和菜单默认值，不自动创建数据库菜单或授权。独立详情页声明 `visible: false`，在菜单管理中配置隐藏页面并按角色明确授权；动态路径支持 `/:userId` 等必填命名参数，不支持通配符、可选参数或自定义正则。
- 后端启动时根据实际 Controller 接口清单清理失效绑定，并在同一事务内递增权限版本、写入审计；接口改名后的新路径需要重新配置绑定，不自动继承旧接口权限。
- 权限及角色关联变更必须在事务内更新数据库权限版本，不能仅依赖 Redis 删除缓存；公共认证接口由固定基础规则管理。
- 管理员通过明确的受保护角色关联识别，禁止根据用户名、角色编码或用户 ID 前缀动态授予管理权限。

## 文件存储

- 当前采用本地磁盘存储，上传目录默认位于项目内的 `crm_serve/storage/uploads/`，与 `src`、`dist` 平级；禁止把上传内容写入构建产物目录。
- 存储位置统一读取 `storage.uploadDirectory`，通过 `UPLOAD_DIR` 覆盖；相对路径基于后端根目录解析，业务代码不硬编码本机绝对路径。
- 实际上传文件不提交 Git，只保留目录占位与说明；部署、备份或迁移时同时处理文件目录及对应数据库记录，构建和发布不得清空上传目录。
- 后续上传模块需校验文件类型、大小和访问权限，由服务端生成存储名称；业务记录保存文件 ID，文件元数据保存相对存储路径，私有附件通过鉴权接口访问。

## 用户 ID 规则

- 新增普通用户的 `userId` 必须为 `crm_user_<UUID>`，管理员账号必须为 `crm_admin_<UUID>`，保留 UUID 中的连字符。
- 用户 ID 由服务端生成，通过 `crm_serve/src/common` 统一导出的 `createUserId()`、`createAdminUserId()` 复用，禁止直接用裸 UUID 创建用户或由前端指定 ID。
- 管理员账号的创建或初始化流程应显式选择管理员 ID 生成方法；禁止根据可编辑的用户名或 ID 前缀授予权限。
- 用户 ID 创建后保持稳定，编辑资料不重新生成 ID；历史 ID 格式调整必须通过显式迁移处理，并同步关联记录及登录会话。

## 修改与验证

- 保留用户未提交的修改，避免无关清理和重构。
- 除非用户明确要求，否则不自行创建测试文件或临时测试脚本（包括 `*.spec.ts`、`*.test.ts` 等），不为新增模块预建 `tests/` 目录；已有测试文件不擅自删除。
- 修改后默认优先运行与影响范围匹配的 typecheck、lint 或静态检查；测试按用户要求执行，并明确报告验证范围和未能执行的验证。
- 非必要情况下不执行完整 build。仅在修改构建配置、应用入口、包导出、依赖或打包链路，需要验证生产产物，或用户明确要求时执行。
- 前端和后端服务默认由用户自行启动、停止和重启；除非用户明确要求，否则不操作本地服务进程。
- 运行态验证依赖服务重启才能反映新代码时，完成静态检查并说明该限制，将重启和运行态验证交由用户处理。
